{
  "name": "random-pic-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for serving random images from R2 storage",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy --env production",
    "dev": "wrangler dev",
    "generate-key": "node script/key-generator.js --test"
  },
  "dependencies": {
    "hono": "^3.12.12"
  },
  "devDependencies": {
    "wrangler": "^3.22.0"
  },
  "private": true
}